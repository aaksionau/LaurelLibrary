@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header text-white" style="background-color: #245C4F;">
                    <h3 class="mb-0">
                        <i class="bi bi-person-plus-fill me-2"></i>Register New Account
                    </h3>
                </div>
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger alert-dismissible fade show"
                        role="alert"></div>

                    <!-- Registration Form Section -->
                    <section class="mb-4">
                        <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                            <div class="mb-3">
                                <label asp-for="Input.FirstName" class="form-label">
                                    <i class="bi bi-person me-1"></i>First Name
                                </label>
                                <input asp-for="Input.FirstName" class="form-control" autocomplete="firstName"
                                    aria-required="true" placeholder="Enter your first name" autofocus />
                                <span asp-validation-for="Input.FirstName" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="Input.LastName" class="form-label">
                                    <i class="bi bi-person me-1"></i>Last Name
                                </label>
                                <input asp-for="Input.LastName" class="form-control" autocomplete="lastName"
                                    aria-required="true" placeholder="Enter your last name" />
                                <span asp-validation-for="Input.LastName" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="Input.Email" class="form-label">
                                    <i class="bi bi-envelope me-1"></i>Email
                                </label>
                                <input asp-for="Input.Email" class="form-control" autocomplete="username"
                                    aria-required="true" placeholder="name@example.com" />
                                <span asp-validation-for="Input.Email" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="Input.Password" class="form-label">
                                    <i class="bi bi-lock me-1"></i>Password
                                </label>
                                <div class="position-relative">
                                    <input asp-for="Input.Password" type="password" class="form-control"
                                        autocomplete="new-password" aria-required="true" placeholder="Create a password"
                                        id="passwordField" />
                                    <button type="button"
                                        class="btn btn-link position-absolute end-0 top-50 translate-middle-y"
                                        id="togglePassword" style="border: none; background: none; padding: 0 15px;">
                                        <i class="bi bi-eye-slash" id="togglePasswordIcon"></i>
                                    </button>
                                </div>
                                <div id="passwordStrength" class="mt-2" style="display: none;">
                                    <div class="small text-muted mb-1">Password must contain:</div>
                                    <ul class="small mb-0" style="list-style: none; padding-left: 0;">
                                        <li id="length-check" class="text-danger"><i class="bi bi-x-circle me-1"></i>At
                                            least 8 characters</li>
                                        <li id="uppercase-check" class="text-danger"><i
                                                class="bi bi-x-circle me-1"></i>One uppercase letter</li>
                                        <li id="lowercase-check" class="text-danger"><i
                                                class="bi bi-x-circle me-1"></i>One lowercase letter</li>
                                        <li id="digit-check" class="text-danger"><i class="bi bi-x-circle me-1"></i>One
                                            digit</li>
                                        <li id="special-check" class="text-danger"><i
                                                class="bi bi-x-circle me-1"></i>One special character</li>
                                    </ul>
                                </div>
                                <span asp-validation-for="Input.Password" class="text-danger"></span>
                            </div>
                            <div class="mb-4">
                                <label asp-for="Input.ConfirmPassword" class="form-label">
                                    <i class="bi bi-lock-fill me-1"></i>Confirm Password
                                </label>
                                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password"
                                    aria-required="true" placeholder="Confirm your password" />
                                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                            </div>
                            <div class="d-grid gap-2">
                                <button id="registerSubmit" type="submit" class="btn btn-lg btn-primary">
                                    <i class="bi bi-check-circle me-2"></i>Register
                                </button>
                            </div>
                        </form>
                    </section>

                    <!-- Separator -->
                    <div class="position-relative my-4">
                        <hr class="w-100" style="border-top: 2px solid #6c757d;" />
                        <span
                            class="position-absolute top-50 start-50 translate-middle bg-white px-3 text-muted fw-semibold">
                            Or register with
                        </span>
                    </div>

                    <!-- External Authentication Section -->
                    <section>
                        <div class="text-center">
                            @{
                                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                                {
                                    <div class="text-muted small">
                                        <p>
                                            <i class="bi bi-info-circle me-1"></i>
                                            No external authentication services configured.
                                        </p>
                                    </div>
                                }
                                else
                                {
                                    <form id="external-account" asp-page="./ExternalLogin"
                                        asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                                        <div class="d-flex justify-content-center">
                                            @foreach (var provider in Model.ExternalLogins!)
                                            {
                                                <button type="submit"
                                                    class="btn btn-light btn-lg d-flex align-items-center justify-content-center gap-3"
                                                    name="provider" value="@provider.Name"
                                                    title="Log in using your @provider.DisplayName account"
                                                    style="border: none; border-radius: 8px; padding: 12px 24px;">
                                                    <i class="bi bi-microsoft" style="font-size: 28px; color: #00A4EF;"></i>
                                                    <span style="font-size: 18px; font-weight: 500;">Microsoft</span>
                                                </button>
                                            }
                                        </div>
                                    </form>
                                }
                            }
                        </div>
                    </section>
                </div>
            </div>

            <div class="text-center mt-3">
                <a asp-page="./Login" class="text-decoration-none">
                    <i class="bi bi-arrow-left me-1"></i>Back to Login
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/register-password-validation.js" asp-append-version="true"></script>
}