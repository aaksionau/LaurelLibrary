@page
@model LaurelLibrary.UI.Areas.Administration.Pages.Subscriptions.SuccessModel
@{
    ViewData["Title"] = "Subscription Success";
}

<!-- This page automatically redirects, but we can add content if needed -->
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-lg" style="background-color: var(--color-bg-light);">
                <div class="card-body text-center py-5">
                    <i class="fas fa-check-circle fa-4x mb-4" style="color: var(--color-success);"></i>
                    <h3 class="mb-3" style="color: var(--color-text-primary); font-weight: 700;">Success!</h3>
                    <p class="mb-3" style="color: var(--color-text-secondary); font-size: 1.1em;">Your subscription has
                        been activated successfully!</p>
                    <p style="color: var(--color-text-secondary);">
                        <i class="fas fa-spinner fa-spin me-2" style="color: var(--color-primary);"></i>
                        Redirecting to subscription management...
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>