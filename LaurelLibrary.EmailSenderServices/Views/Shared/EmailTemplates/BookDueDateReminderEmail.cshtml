@model LaurelLibrary.EmailSenderServices.Dtos.BookDueDateReminderEmailDto

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
        {
            <text>Book Return Reminder - MyLibrarian</text>
        }
        else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
        {
            <text>Books Due Today - MyLibrarian</text>
        }
        else
        {
            <text>Overdue Books Notice - MyLibrarian</text>
        }
    </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #2E2B28;
            background-color: #F9F6F1;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }

        .header {
            background-color: #245C4F;
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }

        .header .subtitle {
            margin-top: 10px;
            font-size: 16px;
            opacity: 0.9;
        }

        .content {
            padding: 40px 30px;
        }

        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #245C4F;
        }

        .message {
            font-size: 16px;
            margin-bottom: 30px;
            line-height: 1.8;
            color: #7A6F66;
        }

        .alert-message {
            @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.Overdue)
            {
                <text>background-color: rgba(220, 53, 69, 0.1); border-left: 4px solid #dc3545; color: #721c24;</text>
            }
            else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
            {
                <text>background-color: rgba(230, 182, 85, 0.1); border-left: 4px solid #E6B655; color: #8a6a14;</text>
            }
            else
            {
                <text>background-color: rgba(36, 92, 79, 0.1); border-left: 4px solid #245C4F; color: #245C4F;</text>
            }
            padding: 15px 20px;
            margin: 20px 0;
            font-weight: 500;
        }

        .books-section {
            margin: 30px 0;
        }

        .section-title {
            font-size: 18px;
            color: #245C4F;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .book-item {
            background-color: #F9F6F1;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 4px solid #245C4F;
        }

        .book-title {
            font-size: 16px;
            font-weight: 600;
            color: #2E2B28;
            margin-bottom: 8px;
        }

        .book-authors {
            font-size: 14px;
            color: #7A6F66;
            margin-bottom: 8px;
        }

        .book-due-date {
            font-size: 14px;
            font-weight: 500;
            @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.Overdue)
            {
                <text>color: #721c24;</text>
            }
            else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
            {
                <text>color: #8a6a14;</text>
            }
            else
            {
                <text>color: #245C4F;</text>
            }
        }

        .book-isbn {
            font-size: 12px;
            color: #7A6F66;
            margin-top: 5px;
        }

        .action-section {
            margin: 30px 0;
        }

        .action-title {
            font-size: 16px;
            font-weight: 600;
            color: #245C4F;
            margin-bottom: 10px;
        }

        .action-text {
            font-size: 14px;
            color: #7A6F66;
            line-height: 1.6;
        }

        .footer {
            background-color: #F9F6F1;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #e8e3db;
            font-size: 14px;
            color: #7A6F66;
        }

        .footer .library-info {
            margin-bottom: 15px;
            font-weight: 600;
            color: #2E2B28;
        }

        .library-details {
            background-color: #F9F6F1;
            padding: 20px;
            margin: 30px 0;
            color: #7A6F66;
        }

        .library-name {
            font-size: 16px;
            font-weight: 600;
            color: #2E2B28;
            margin-bottom: 10px;
        }
    </style>
            padding: 15px 20px;
            margin: 20px 0;
            font-weight: 500;
        }

        .books-section {
            margin: 30px 0;
        }

        .section-title {
            font-size: 18px;
            color: #245C4F;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .book-item {
            background-color: #F9F6F1;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 4px solid #245C4F;
        }

        .book-title {
            font-size: 16px;
            font-weight: 600;
            color: #2E2B28;
            margin-bottom: 8px;
        }

        .book-authors {
            font-size: 14px;
            color: #7A6F66;
            margin-bottom: 8px;
        }

        .book-due-date {
            font-size: 14px;
            font-weight: 500;
            @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.Overdue)
            {
                <text>color: #721c24;</text>
            }
            else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
            {
                <text>color: #8a6a14;</text>
            }
            else
            {
                <text>color: #245C4F;</text>
            }
        }

        .book-isbn {
            font-size: 12px;
            color: #7A6F66;
            margin-top: 5px;
        }

        .action-section {
            margin: 30px 0;
        }

        .action-title {
            font-size: 16px;
            font-weight: 600;
            color: #245C4F;
            margin-bottom: 10px;
        }

        .action-text {
            font-size: 14px;
            color: #7A6F66;
            line-height: 1.6;
        }

        .footer {
            background-color: #F9F6F1;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #e8e3db;
            font-size: 14px;
            color: #7A6F66;
        }

        .footer .library-info {
            margin-bottom: 15px;
            font-weight: 600;
            color: #2E2B28;
        }

        .library-details {
            background-color: #F9F6F1;
            padding: 20px;
            margin: 30px 0;
            color: #7A6F66;
        }

        .library-name {
            font-size: 16px;
            font-weight: 600;
            color: #2E2B28;
            margin-bottom: 10px;
        }
        }

        .action-title {
            font-size: 16px;
            font-weight: 600;
            color: #245C4F;
            margin-bottom: 10px;
        }

        .action-text {
            font-size: 14px;
            color: #7A6F66;
            line-height: 1.6;
        }

        .footer {
            background-color: #F9F6F1;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #e8e3db;
            font-size: 14px;
            color: #7A6F66;
        }

        .footer .library-info {
            margin-bottom: 15px;
            font-weight: 600;
            color: #2E2B28;
        }

        .library-details {
            background-color: #F9F6F1;
            padding: 20px;
            margin: 30px 0;
            color: #7A6F66;
        }

        .library-name {
            font-size: 16px;
            font-weight: 600;
            color: #2E2B28;
            margin-bottom: 10px;
        }

        .divider {
            height: 1px;
            background-color: #e8e3db;
            margin: 30px 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>MyLibrarian</h1>
            <div class="subtitle">
                @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                {
                    <text>Book Return Reminder</text>
                }
                else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                {
                    <text>Books Due Today</text>
                }
                else
                {
                    <text>Overdue Books Notice</text>
                }
            </div>
        </div>

        <div class="content">
            <div class="greeting">
                Hello @Model.ReaderName,
            </div>

            <div class="alert-message">
                @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                {
                    <text><strong>Friendly Reminder:</strong> You have @Model.Books.Count book(s) that will be due in 3 days. Please plan to return them on time to avoid late fees.</text>
                }
                else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                {
                    <text><strong>Due Today:</strong> You have @Model.Books.Count book(s) that are due for return today. Please return them as soon as possible.</text>
                }
                else
                {
                    <text><strong>Overdue Notice:</strong> You have @Model.Books.Count book(s) that are overdue. Please return them immediately to avoid additional late fees.</text>
                }
            </div>

            <div class="message">
                @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                {
                    <text>This is a friendly reminder that you have books checked out from our library that will be due soon. We wanted to give you advance notice so you can plan your return accordingly.</text>
                }
                else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                {
                    <text>Your borrowed books are due for return today. Please bring them to the library during our operating hours to avoid any late fees.</text>
                }
                else
                {
                    <text>Our records show that you have overdue books. Please return them as soon as possible. Late fees may apply for overdue items.</text>
                }
            </div>

            <div class="books-section">
                <div class="section-title">
                    @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                    {
                        <text>Books Due Soon</text>
                    }
                    else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                    {
                        <text>Books Due Today</text>
                    }
                    else
                    {
                        <text>Overdue Books</text>
                    }
                </div>

                @foreach (var book in Model.Books)
                {
                    <div class="book-item">
                        <div class="book-title">@book.Title</div>
                        @if (!string.IsNullOrEmpty(book.Authors))
                        {
                            <div class="book-authors">by @book.Authors</div>
                        }
                        <div class="book-due-date">
                            @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                            {
                                <text>Due: @book.DueDate.ToString("MMMM dd, yyyy") (in @Math.Abs(book.DaysOverdue) days)</text>
                            }
                            else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                            {
                                <text>Due: @book.DueDate.ToString("MMMM dd, yyyy") (TODAY)</text>
                            }
                            else
                            {
                                <text>Was due: @book.DueDate.ToString("MMMM dd, yyyy") (@book.DaysOverdue days overdue)</text>
                            }
                        </div>
                        @if (!string.IsNullOrEmpty(book.Isbn))
                        {
                            <div class="book-isbn">ISBN: @book.Isbn</div>
                        }
                    </div>
                }
            </div>

            <div class="action-section">
                <div class="action-title">
                    @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.Overdue)
                    {
                        <text>Immediate Action Required</text>
                    }
                    else
                    {
                        <text>What You Need to Do</text>
                    }
                </div>
                <div class="action-text">
                    @if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.UpcomingDueDate)
                    {
                        <text>Please plan to return your books by the due date. You can renew books online or by visiting the library if you need more time.</text>
                    }
                    else if (Model.ReminderType == LaurelLibrary.EmailSenderServices.Dtos.ReminderType.DueToday)
                    {
                        <text>Please return your books today during library hours. If you can't make it today, please contact us to discuss renewal options.</text>
                    }
                    else
                    {
                        <text>Please return your overdue books immediately. Contact the library if you have any questions about your account or need assistance with returns.</text>
                    }
                </div>
            </div>

            <div class="divider"></div>

            <div class="library-details">
                <div class="library-name">@Model.LibraryName</div>
                @if (!string.IsNullOrEmpty(Model.LibraryDescription))
                {
                    <div>@Model.LibraryDescription</div>
                }
                @if (!string.IsNullOrEmpty(Model.LibraryAddress))
                {
                    <div><strong>Address:</strong> @Model.LibraryAddress</div>
                }
            </div>

            <div class="message">
                Thank you for being a valued member of our library community. If you have any questions or need assistance, please don't hesitate to contact us.
            </div>
        </div>

        <div class="footer">
            <div class="library-info">
                MyLibrarian System
            </div>
            <div>
                This is an automated message from MyLibrarian.
                Please do not reply to this email.
            </div>
            <div style="margin-top: 15px; font-size: 12px; color: #adb5bd;">
                © @DateTime.Now.Year MyLibrarian. All rights reserved.
            </div>
        </div>
    </div>
</body>

</html>